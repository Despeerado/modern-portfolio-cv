{
  "name": "virtual-cv",
  "version": "1.0.0",
  "description": "Online životopis a portfolio",
  "main": "index.js",
  "scripts": {
    "sass": "sass scss/main.scss:css/style.css",
    "sass:watch": "sass --watch scss/main.scss:css/style.css",
    "sass:compressed": "sass --style=compressed scss/main.scss:css/style.min.css",
    "bootstrap": "sass scss/main.scss:css/custom-bootstrap.css --style=compressed",
    "bootstrap:watch": "sass --watch scss/main.scss:css/custom-bootstrap.css --style=compressed",
    "build": "npm run sass:compressed && npm run bootstrap"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.6"
  },
  "devDependencies": {
    "sass": "^1.89.1"
  }
}
